// UI photo upload is covered by Playwright E2E (real browser + canvas).
// For unit-level coverage, see utils/image.test.ts for pure resize math and
// features/quests/questStore.test.ts for seeded photo proof persistence.

describe.skip('QuestDetail photo proof submission (covered by E2E)', () => {
	it('is tested in E2E', () => {
		// intentionally empty; prevents Vitest from failing on an empty file
	});
});
